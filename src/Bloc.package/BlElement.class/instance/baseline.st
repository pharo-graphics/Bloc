geometry position
baseline
	"Return the offset of the element's text baseline from the element's top boundary.
	If I don't support baseline alignment, throw BlBaselineUnsupported error"
	<return: #Number>
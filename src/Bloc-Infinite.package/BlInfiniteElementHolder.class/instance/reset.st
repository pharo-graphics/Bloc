private - invalidate
reset

	flags clear.
	position := NoPosition.
	previousPosition := NoPosition.
	itemId := NoId.
	preLayoutPosition := NoPosition.
	recyclableCount := 0.
	shadowedHolder := nil.
	shadowingHolder := nil.
	self clearPayload
Class {
	#name : #BlDevScripterSetStepStorage,
	#superclass : #Object,
	#category : #'Bloc-Scripter-Scripter-Storage'
}

{ #category : #storing }
BlDevScripterSetStepStorage >> storeFrom: aTarget scripter: aScripter [
	"Target can be an element, scripter, model, etc., depending on used ${class:BlDevScripterTarget}$."
]
